#ifndef PPS_MULTI_ARRAY_DIMENSIONS_H#define PPS_MULTI_ARRAY_DIMENSIONS_H#include <vector>namespace pps {namespace multi_array {/*class dimensions{	private:		std::vector<std::size_t> m_data;	public:		dimensions();		dimensions(const dimensions& p_dimensions);		std::size_t size() const;		std::size_t last() const;		dimensions& operator=(const dimensions& p_dimensions);		dimensions& operator[](std::size_t p_dimension);		dimensions parent() const;};*/}}#endif