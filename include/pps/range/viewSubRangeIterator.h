#ifndef PPS_RANGE_VIEW_SUB_RANGE_ITERATOR_H#define PPS_RANGE_VIEW_SUB_RANGE_ITERATOR_H#include <iterator>namespace pps {namespace range {class viewSubRangeIterator	: public std::iterator<std::input_iterator_tag, std::size_t>{	private:		std::size_t m_index;		std::size_t m_increment;	public:		viewSubRangeIterator(std::size_t p_index, std::size_t p_increment = 1);		viewSubRangeIterator(const viewSubRangeIterator& p_iterator);		viewSubRangeIterator& operator=(const viewSubRangeIterator& p_iterator);		bool operator==(const viewSubRangeIterator& p_iterator) const;		bool operator!=(const viewSubRangeIterator& p_iterator) const;		std::size_t operator*() const;		viewSubRangeIterator& operator++();		viewSubRangeIterator operator++(int);};}}#endif